name: Test Changelog [TEMP]

on:
  push:
    branches: [ "**" ]
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: read

jobs:
  changelog:
    name: Test Changelog
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Test Changelog
      run: python .github/scripts/changelog.py